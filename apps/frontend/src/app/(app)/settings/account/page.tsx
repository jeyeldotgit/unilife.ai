import ProfileClient from "@/app/(app)/profile/ProfileClient";

export default function AccountSettingsPage() {
  return <ProfileClient pageTitle="Account Settings" showOperationalPanels={false} />;
}
